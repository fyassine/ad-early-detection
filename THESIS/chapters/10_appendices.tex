% !TeX root = ../main.tex

\chapter{Mathematical Derivations and Loss Formulations}\label{ch:app-derivations}

\chapter{Implementation Details}\label{ch:app-implementation}

\section*{Brain-TokenGT: Released-Code Behaviours and the Stabilisation Configuration}

\autoref{sec:braintokengt} characterises the recurrent core of the Brain-TokenGT
comparator across three configurations. This section states the remaining configuration
changes present in the version of the released code evaluated here, and the exact numeric
settings of the three configurations named in \autoref{sec:braintokengt}.

Four behaviours of the released implementation contradict statements made in the published
architecture description. Each is controlled by a configuration flag whose default value
reproduces the released behaviour, so a run at every flag's default is the faithful
reproduction of the released code:

\begin{itemize}
  \item The released code discards the connectivity-derived edge weights before they reach
  the model's hypergraph convolution, so that layer receives a vector of ones regardless of
  the underlying connectivity strength. A configuration flag controls whether edge weights
  are forwarded; its default reproduces the released behaviour.
  \item The released code reads out the graph-level representation by averaging every
  token in the sequence, whereas the architecture description reads out a single dedicated
  graph token. A configuration flag selects between the two; its default reproduces the
  released behaviour.
  \item The released code sets the number of attention heads and then immediately
  overwrites it with a single head, discarding the configured head count. A configuration
  flag controls whether this override is applied; its default reproduces the released
  behaviour.
  \item The released code's parameter-registration mechanism for the recurrent core returns
  a plain tensor rather than a registered parameter, so the recurrent weights are never
  visible to the optimiser and remain at their random initialisation regardless of what
  training is requested. A configuration flag controls whether the recurrent weights are
  registered with the optimiser; its default reproduces the released behaviour.
\end{itemize}

The three recurrent-core configurations described in \autoref{sec:braintokengt} are, in the
numeric settings of the shared configuration file: the released configuration leaves all
four flags above at their default; the second and third configurations both set every flag
above to its non-default value, so that the recurrent weights are registered with the
optimiser and the released implementation's other three departures from the published
architecture are corrected at the same time. The third configuration additionally places
the recurrent parameters in a separate optimiser group with a learning-rate multiplier of
0.1 and a weight decay of $10^{-3}$, while every other trainable parameter keeps the base
learning rate of $10^{-3}$ and zero weight decay. Every configuration uses the Adam
optimiser, a batch size of 8, gradient-norm clipping at 1.0, up to 50 epochs with early
stopping at patience 15, and cost-sensitive class weighting, matching the training
procedure applied to every other configuration in \autoref{sec:protocol}.

\section*{Gradient-Control Modification for Encoder Ablation}

The inherited implementation embedded every visit inside \texttt{torch.no\_grad()} with
forced evaluation mode, so the pre-existing \texttt{freeze\_encoder: false} configuration
could not fine-tune the encoder regardless of what \texttt{requires\_grad} indicated. A
new \texttt{EvalConfig.encoder\_grad} flag, defaulting to \texttt{False}, opens that
context for the training pass of the \texttt{pretrained\_finetuned} and \texttt{random}
configurations only. Evaluation always embeds under no-grad with the encoder in evaluation mode. An automated unit test in the test suite pins that this flag is what makes the \texttt{random} configuration meaningful: without it, the model
builds a randomly initialised encoder but cannot update its weights.


\chapter{Reproducibility Table}\label{ch:app-reproducibility}
\outlinenote{Experiment identifiers, run identifiers, registry entries and code paths, so
the main text cites one table instead of interrupting the narrative.}

\section*{A recorded metadata defect}

For every run completed through 22 August 2026, the persisted training-configuration
record's seed field reads 42 regardless of the seed the run was actually launched with,
affecting approximately sixty saved run artefacts across the experiments reported in
\autoref{ch:results}. The four-seed identity of each such run is recovered from its
experiment identifier (for example, the \texttt{-seed43} suffix on a registry entry) rather
than from this field. The defect was identified after these runs completed and was
deliberately not corrected retroactively in the saved artefacts, to avoid rewriting a
persisted record after the fact; every seed-level statistic in \autoref{ch:results} is
computed from the experiment identifier's seed rather than from this field.

\chapter{Split Integrity and Overlap Verification Logs}\label{ch:app-splits}

\autoref{sec:leakage} states the split-integrity contract every cohort's splits are built
against and the historical containment failure that motivated part of it. This appendix
records that contract as a specification: the audit module's exact checks, and the full
list of invariants the automated test suite pins against every committed split file. The
split and manifest files these checks run against are generated on the shared training
infrastructure described in \autoref{ch:app-environment} rather than committed to the
project repository, so what follows is the audit's specification rather than a captured
run of its console output; a live run against a current set of split files is deferred to
the training environment where those files exist.

\section*{Audit module}

Four checks, each raising a descriptive error on the first violation found rather than
returning a boolean:

\begin{itemize}
  \item \textbf{Pairwise split disjointness.} Given a named collection of split files and a
  subject-identifier column, asserts every pair of splits shares no subject identifiers.
  On failure, the error names the offending pair of splits, the number of shared subjects,
  and up to five example shared identifiers.
  \item \textbf{No duplicate connectivity matrices.} Given the connectivity-matrix file
  referenced by every row of a split, hashes each matrix's array contents and asserts no
  two distinct file paths hash identically, catching a matrix accidentally duplicated
  across two subjects or two visits.
  \item \textbf{Group-aware fold audit.} Re-runs the stratified, subject-grouped
  cross-validation fold assignment for a given set of subject identifiers and labels, and
  asserts that no subject's visits appear in more than one fold's validation set.
  \item \textbf{Cohort containment policy.} Given a self-supervised pretraining cohort's
  subject identifiers and a downstream cohort's subject identifiers, checks them against a
  stated policy: under a disjoint policy, any overlap raises; under a shared policy, overlap
  is permitted and no check is enforced, on the reasoning that some exploratory notebooks
  legitimately reuse a subset of subjects across both roles. This is the check that directly
  targets the historical containment failure described in \autoref{sec:leakage} when it is
  invoked with the disjoint policy.
\end{itemize}

A single audit entry point runs the pairwise-disjointness check against whichever split
dictionary it is called with. It does not currently chain the duplicate-matrix check, the
fold audit, or the cohort-containment check into one call; a caller wanting those
guarantees has to invoke them separately. \autoref{sec:leakage} states this as a limitation
of the audit as it currently stands, since every notebook that calls the single entry point
receives only the disjointness guarantee rather than the full contract this appendix
describes.

\section*{Test suite invariants}

The following invariants are pinned by an automated test suite run against every committed
split file, grouped by what they check:

\begin{itemize}
  \item \emph{Schema and completeness.} Required columns are present in both the
  pretraining and downstream split schemas; key columns carry no null values in the
  downstream schema; every subject retained in either schema has at least one usable scan.
  \item \emph{Label consistency.} A converter's post-conversion visits are excluded from
  their allowed-visit set; a subject's converter status is encoded consistently as a binary
  value; the number of scans recorded for a subject matches the number of visits in their
  allowed-visit set; subjective cognitive decline, a diagnostic category outside the
  modelled scope, never appears in either schema.
  \item \emph{Intra-split integrity.} Neither the pretraining nor the downstream schema
  contains duplicate rows within a single split.
  \item \emph{Inter-split disjointness.} The training, validation and test subsets of the
  pretraining schema are pairwise disjoint; the same holds for the downstream schema.
  \item \emph{Cross-schema containment.} The pretraining training set is disjoint from the
  downstream validation set; the pretraining training set is disjoint from the downstream
  test set; the pretraining validation set is disjoint from the downstream test set; the
  downstream validation set is a subset of the pretraining validation set; the downstream
  test set is a subset of the pretraining test set. These five checks are the direct
  descendants of the historical containment failure described in \autoref{sec:leakage}.
  \item \emph{Balance and coverage.} Class balance across the downstream schema's splits,
  and subject coverage across the pretraining schema's free (non-stratified) split, stay
  within a stated tolerance; every subject referenced by either schema is present in the
  corresponding subject-information table.
  \item \emph{Smoke tests.} The audit entry point runs without error against both the
  downstream and the pretraining split files.
\end{itemize}

\chapter{Complete Cross-Validation Metrics}\label{ch:app-metrics}

\chapter{Software Dependencies and Hardware Environment}\label{ch:app-environment}
