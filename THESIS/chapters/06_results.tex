% !TeX root = ../main.tex

\chapter{Experimental Results and Ablations}\label{ch:results}

% Scope rule for this chapter: report what happened, ordered by research question.
% Interpretation belongs in Chapter 7.

\section{Reporting Rules}\label{sec:reporting-rules}

This chapter reports what happened, organised by the four research questions stated in
\autoref{sec:research-questions}; interpretation of why a result occurred is deferred to
\autoref{ch:discussion}. This section fixes the conventions that every later section
relies on rather than restating them at each occurrence.

\paragraph{Two evaluation universes.} \autoref{sec:rq1}, \autoref{sec:rq3} and
\autoref{sec:rq4} report the temporal-first ablation sequence introduced in
\autoref{sec:tfgn}, evaluated on the pooled \ac{ADNI}-and-\ac{DELCODE} protocol of
\autoref{sec:pooled-protocol} at $n=248$ subjects. \autoref{sec:rq2} reports the
spatial-first encoder ablation, evaluated on the \ac{DELCODE}-only protocol with its own
independent test split. The two are never read as like-for-like rows against one another;
where a number from one case is mentioned in the other, its source protocol is named
explicitly.

\paragraph{Architectural allocation across sections.} The primary comparative evaluations (\autoref{sec:rq1}, \autoref{sec:rq3}, and \autoref{sec:rq4}) focus on the headline temporal-first ablation sequence (configurations S1 through S5) and Brain-TokenGT. In contrast, \autoref{sec:rq2}, the latent geometry analysis of \autoref{sec:interpretability-results}, and the perturbation sensitivity checks of \autoref{sec:robustness-results} deliberately evaluate the representation and robustness properties of the baseline spatial-first architectures (\ac{GAAE}, \ac{GELSTM}, and \ac{GEGRU}). These later sections serve as targeted diagnostic probes into specific legacy representation mechanisms rather than competing full-cohort models.


\paragraph{Primary metric and independent unit.} The primary metric is the pooled
out-of-fold \ac{AUC} over the full cross-validation pool, reported as the mean and
standard deviation across the four seeds 42, 43, 44 and 45. The independent unit for any
seed-level statistic is the seed, not the fold: folds drawn from one seed share a trained
model and are not independent draws, so a comparison across the twenty folds produced by
five-fold cross-validation at four seeds is never treated as twenty independent
observations. Balanced accuracy and the per-cohort \ac{AUC} for \ac{ADNI} and \ac{DELCODE}
separately accompany every headline row, since \autoref{sec:rq2} and \autoref{sec:rq4}
both report configurations whose two cohorts differ substantially in difficulty, and a pooled number
alone would average over that difference rather than expose it.

\paragraph{Class imbalance and threshold policy.} Converters are a minority of the cohort
in all three datasets, which is why cost-sensitive class weighting is applied to the
training loss rather than resampling, and why the decision threshold is selected by
maximising F1 on out-of-fold predictions and reused unchanged on the test set, as fixed in
\autoref{sec:protocol}.

\paragraph{Resolvability.} At the in-domain test size of $n=64$ and the external size of $n=60$, the empirical standard error of the \ac{AUC} is $\text{SE} \approx 0.04\text{--}0.045$, giving a 95\% confidence interval half-width ($1.96 \times \text{SE}$) of approximately $0.08\text{--}0.09$. Consequently, \ac{AUC} differences below approximately $0.08$ fall entirely inside the random sampling noise margin and cannot be resolved with bootstrap confidence intervals. On the four-fold larger out-of-fold pool ($n=248$), where sample error scales with $1/\sqrt{N}$, the standard error and confidence interval width are halved to approximately $0.04$, as derived in \autoref{sec:protocol}. Every keep-or-drop verdict reported below is therefore a screening heuristic at four seeds, stating whether an effect is detectable at this sample size, never a significance test and never a claim that an undetected effect is absent.

\paragraph{Seed identity.} Every seed-level statistic in this chapter is computed against
the seed named in its experiment identifier. \autoref{ch:app-reproducibility} records a
metadata defect affecting a subset of the underlying run artefacts.

\section{RQ1: Does Longitudinal Modelling Add Information Beyond Static Representations?}\label{sec:rq1}

\autoref{tab:rq1-floors} reports the three floors from \autoref{sec:baseline-sequence} on the
pooled protocol, alongside the selected temporal-first configuration S1 introduced in
\autoref{sec:protocol}. The demographics-only floor sits at 0.5296, indistinguishable from
chance, confirming that age and sex alone carry no separable signal in this pooled cohort.
The static single-visit floor, evaluated per fold on S1's own configuration without a
trajectory, scores 0.4919, likewise indistinguishable from chance. The linear
logistic-on-drift floor, which reads the flattened connectivity-change matrix directly with
no learned representation, reaches 0.7053, already close to the fully trained temporal-first
configuration's 0.7488.

\begin{table}[htbp]
\centering
\caption{Pooled out-of-fold \ac{AUC} (mean $\pm$ standard deviation across four seeds) for
  the three floors and the selected temporal-first configuration S1.}
\label{tab:rq1-floors}
\begin{tabular}{@{}lc@{}}
\toprule
Configuration & Pooled out-of-fold \ac{AUC} \\
\midrule
Demographics only (age, sex) & $0.5296 \pm 0.0000$ \\
Static single visit (S1 configuration, per-fold probe) & $0.4919$ \\
Linear logistic regression on connectivity drift & $0.7053 \pm 0.0000$ \\
S1 (full trajectory) & $0.7488 \pm 0.0033$ \\
\bottomrule
\end{tabular}
\end{table}

Two readings follow directly from this table. First, all of the signal available to the
selected configuration is longitudinal: its own single-visit evaluation is at chance,
against 0.7488 on full trajectories, a gap of 0.257 \ac{AUC} that cannot be attributed to
age, sex or any single-scan connectivity pattern. Second, the linear floor already recovers
most of the fully trained configuration's advantage over the single-visit floor, which
constrains how much of the remaining gap a nonlinear temporal model can plausibly be
credited with; \autoref{ch:discussion} returns to this comparison alongside the capacity
accounting for S1.

\paragraph{Sequence-length decomposition.} \autoref{tab:rq1-sens} restricts S1's own
out-of-fold predictions to the 140 subjects with at least three visits, the same subset the
SENS configuration of \autoref{sec:protocol} trains on, and reports this restricted score
alongside SENS's own out-of-fold \ac{AUC} at each seed. Because SENS's 140
subjects are a strict subset of S1's 248, the two rows separate two effects that a
single SENS number alone would conflate.

\begin{table}[htbp]
\centering
\caption{Sequence-length decomposition: S1's own out-of-fold predictions restricted to
  the 140 subjects with at least three visits, against SENS, trained only on that
  140-subject pool. Both are out-of-fold \ac{AUC}.}
\label{tab:rq1-sens}
\begin{tabular}{@{}lcccc@{}}
\toprule
Seed & 42 & 43 & 44 & 45 \\
\midrule
S1 restricted to $\geq 3$ visits & 0.7762 & 0.7656 & 0.7603 & 0.7825 \\
SENS (trained on $\geq 3$ visits only) & 0.7385 & 0.7499 & 0.7230 & 0.7535 \\
\bottomrule
\end{tabular}
\end{table}

Restricting S1's own predictions to the 140-subject subgroup raises its mean
out-of-fold \ac{AUC} from 0.7488 to 0.7712, a difference of $+0.022$: subjects with three or
more visits are easier to classify than the full pool, which is the sequence-length signal
the SENS configuration was designed to probe. However, training exclusively on these 140 subjects
yields 0.7412 (a difference of $-0.030$). The performance penalty from smaller training data
exceeds the gain from longer visit histories. As a result, SENS and the full-cohort baseline
are not directly comparable due to differences in sample size, subject inclusion, and fold splits.

\section{RQ2: Does Spatial-First Graph Representation Learning Add Value?}\label{sec:rq2}

This section reports the \ac{DELCODE}-only encoder ablation on the spatial-first
\ac{GELSTM}, introduced as an instance of \autoref{sec:spatial-first}, together with the
reconstruction-fidelity measurement that quantifies what the pretraining objective actually
reconstructs. It is the only section of this chapter that departs from the pooled protocol
named in \autoref{sec:reporting-rules}: it uses the \ac{DELCODE}-only downstream split of
167 subjects, with a fixed 34-subject held-out test set at the configuration's own
best-F1-derived threshold. We evaluate mean versus attentive pooling in \autoref{sec:rq3}
(configuration S4) under the pooled protocol rather than here, as the static graph-embedding classifier
is excluded due to configuration confounds and high run-to-run instability (detailed at the
end of this section).

\paragraph{Encoder ablation.} \autoref{tab:rq2-encoder} reports four initialisations of the
same \ac{GELSTM} architecture, differing only in how the encoder is initialised: no encoder
at all, with the pooled node features passed directly to the recurrent core; a
reconstruction-pretrained encoder held frozen; a randomly initialised encoder trained end to
end with the classification gradient; and the same reconstruction-pretrained encoder
unfrozen and fine-tuned jointly with the classification gradient.

\begin{table}[htbp]
\centering
\small
\caption{\ac{DELCODE}-only encoder ablation for the spatial-first \ac{GELSTM}. Held-out
  test \ac{AUC} is the mean across four seeds (sample standard deviation, $n-1$
  denominator); cross-validation \ac{AUC} pools every completed run. Trainable parameter
  counts are architecture-derived, not sample statistics.}
\label{tab:rq2-encoder}
\begin{tabular}{@{}p{4.3cm}ccc@{}}
\toprule
Encoder initialisation & Test \ac{AUC} & \ac{CV} \ac{AUC} & Trainable params.\ \\
\midrule
None & $0.8313 \pm 0.0529$ & $0.8932 \pm 0.0073$ & 31{,}169 \\
Reconstruction-pretrained, frozen & $0.7812 \pm 0.0122$ & $0.9211 \pm 0.0087$ & 520{,}905 \\
Randomly init., trained end to end & $0.7312 \pm 0.1113$ & $0.7676 \pm 0.0514$ & 965{,}897 \\
Reconstruction-pretrained, fine-tuned & $0.6924 \pm 0.2000$ & $0.8183 \pm 0.0222$ & 965{,}897 \\
\bottomrule
\end{tabular}
\end{table}

A metadata-only floor (age, sex and visit timing, gradient-boosted trees) scores
$0.6157 \pm 0.0653$ cross-validation \ac{AUC} and $0.4929$ test \ac{AUC} on this same split;
every configuration in \autoref{tab:rq2-encoder} clears it by a wide margin on cross-validation
\ac{AUC}, so all four are reading imaging information rather than demographics alone.

Two comparisons follow directly from this table. First, the no-encoder variant reaches the
highest test \ac{AUC} and the highest cross-validation \ac{AUC} within fold-to-fold spread
of the frozen pretrained model ($0.8932$ against $0.9211$), while using approximately
seventeen times fewer trainable parameters ($31{,}169$ against $520{,}905$): on this cohort
size, the graph encoder is not adding value over mean-pooled raw connectivity features for
this architecture. Second, the randomly initialised model scores lowest on cross-validation
\ac{AUC} of the four and carries the largest spread, so reconstruction pretraining does
outperform training the same encoder from a random initialisation when it is trained end to
end; since the no-encoder variant also outperforms the randomly initialised one, this does not
by itself support a claim that the reconstruction objective transfers a generally useful
representation, only that it stabilises optimisation relative to a random start.

\paragraph{Fine-tuning instability.} When fine-tuning the encoder on the small downstream dataset, the test performance swing across seeds is substantial:
\begin{itemize}
  \item \textbf{Seeds 44 and 45} reach held-out test \ac{AUC} values of $0.8375$ (two repeats) and $0.8535$ (one run), respectively.
  \item \textbf{Seed 42} yields a test \ac{AUC} of $0.6535$ (mean of three repeats).
  \item \textbf{Seed 43} falls to a test \ac{AUC} of $0.4250$ (two repeats, an exact replicate), below the chance value of $0.50$.
\end{itemize}
Unlike Brain-TokenGT, where running the same seed produces substantial run-to-run scatter due to hardware-level non-determinism (\autoref{sec:reproducibility-audit}), this fine-tuning collapse is 100\% bit-reproducible: repeating seed 43 yields the exact same test \ac{AUC} of $0.4250$ across independent runs and across distinct code commits. The spread across seeds is therefore a reproducible property of the seed rather than run-to-run execution noise. We illustrate the fold-level cross-validation and seed-level held-out test distributions for both \ac{GELSTM} and Brain-TokenGT on the matched longitudinal window ($T \in [2, 3]$) in \autoref{fig:rq2-cv-test-distributions}, showing the cross-validation spread alongside the seed-to-seed test variability.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{fig1_cv_and_test_distributions}
  \caption{Cross-validation and held-out test metric distributions across seeds on the matched longitudinal window ($T \in [2, 3]$). (A) Five-fold cross-validation \ac{AUC} distribution ($n=20$ folds) for \ac{GELSTM} with a frozen pretrained encoder and Brain-TokenGT. (B) Held-out test \ac{AUC} across the four evaluated seeds (42, 43, 44, and 45) with dashed lines connecting seed-matched pairs. (C) Held-out test F1 scores across the same four seeds.}
  \label{fig:rq2-cv-test-distributions}
\end{figure}

Unfreezing the encoder raises the number of trainable parameters to approximately
$966{,}000$ against roughly $106$ training subjects per fold, and the training
configuration applies a single learning rate across the entire architecture. Candidate explanations for
the spread are discussed in \autoref{sec:ssl-contribution}; no configuration in this ablation
sequence isolates any of them.

\paragraph{Reconstruction fidelity.} The reconstruction-fidelity measurement quantifies what
each encoder's decoder reproduces, using the scale-free Pearson correlation between the
input and its reconstruction, computed over every flattened element rather than averaged
per row. For the \ac{GAAE} encoder the target is the continuous, Fisher z-transformed
connectivity feature matrix; for two anticollapse-regularised \ac{VGAE} variants
(convolutional and attention-based graph encoders) the same statistic is reported for both
the binary adjacency reconstruction and, when the model has a feature decoder, the same
continuous feature target used for \ac{GAAE}. Because the adjacency and feature targets
differ in difficulty, only the feature-reconstruction numbers are comparable across the two
architectures.

\begin{table}[htbp]
\centering
\small
\caption{Reconstruction-fidelity Pearson correlation on \ac{DELCODE} whole-brain data,
  reported as median [25th, 75th percentile].}
\label{tab:rq2-fidelity}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}lcccc@{}}
\toprule
& \multicolumn{2}{c}{Feature fidelity} & \multicolumn{2}{c}{Adjacency fidelity} \\
Encoder & Train+val & Test & Train+val & Test \\
\midrule
\ac{GAAE} & $0.061\ [0.014,\,0.109]$ & $0.046\ [0.026,\,0.109]$ & $-0.121\ [-0.151,\,-0.087]$ & $-0.116\ [-0.140,\,-0.079]$ \\
\ac{VGAE}, graph conv.\ & $-0.088\ [-0.181,\,0.014]$ & $-0.008\ [-0.090,\,0.121]$ & $0.341\ [0.316,\,0.357]$ & $0.319\ [0.302,\,0.338]$ \\
\ac{VGAE}, graph attn.\ & $-0.028\ [-0.127,\,0.066]$ & $0.028\ [-0.072,\,0.103]$ & $0.336\ [0.303,\,0.364]$ & $0.331\ [0.306,\,0.347]$ \\
\bottomrule
\end{tabular}%
}
\vspace{0.75em}
\end{table}

On the comparable feature-reconstruction target, \ac{GAAE}'s reconstruction is weakly
positive while both \ac{VGAE} variants' feature reconstruction is at or below zero,
qualifying as poor by the reconstruction-quality convention adopted throughout, under which
a correlation at or above 0.90 is labelled excellent and below 0.60 is labelled poor. On adjacency reconstruction, the results of \ac{VGAE} are better than those of \ac{GAAE}, where \ac{GAAE}'s inner-product decoder achieves a negative correlation ($-0.12$). The adjacency-reconstruction correlation reported for \ac{VGAE} is substantially higher
(0.32 to 0.34) but is not evidence of a better connectivity reconstruction, since a sparse
binary adjacency is an easier target for a decoder that predicts the majority class for most
entries; it is reported here only because it is the target the \ac{VGAE} decoder head used
in \autoref{sec:instrumentation}'s pretraining actually optimises.

\paragraph{Why the graph-embedding classifiers are not reported.} An initial characterisation
of the \ac{GAAE} encoder used a static, single-visit graph-embedding classifier
(\ac{GEC}) rather than the trajectory-based \ac{GELSTM} ablation above. This comparison is
excluded from this thesis: what this line of investigation established
before being set aside is a qualitative finding rather than a classification number:
statistical characterisation of the raw connectivity matrices found that within-cohort
between-patient variation exceeds between-cohort variation by a wide margin, so a
reconstruction objective evaluated at this cohort size spends its capacity on subject
idiosyncrasy rather than on the disease-relevant signal the encoder ablation above tests
directly. This observation is what motivated moving from an encoder-centred evaluation to the
embed-then-classify comparison reported in this section; it was the connectivity statistics
themselves, rather than any \ac{GEC} classification score, that prompted the change.

\section{RQ3: Does Temporal-First Modelling Improve over Spatial-First Modelling?}\label{sec:rq3}

This section reports the temporal-first ablation sequence of \autoref{sec:protocol} on the
pooled protocol: the scorecard across every configuration, the Tier-2 keep-or-drop verdict for each
configuration against S1, the parameter-count comparison motivating why the result cannot be
attributed to capacity, and the matched-window comparison against the short-window comparator.

\subsection{Scorecard and Keep-or-Drop Verdicts}\label{sec:rq3-scorecard-verdicts}

\paragraph{Scorecard.} \autoref{tab:rq3-scorecard} reports pooled out-of-fold \ac{AUC} for
every configuration defined in \autoref{sec:protocol}. All values are mean $\pm$ standard deviation
across seeds 42 to 45, except where noted.

\begin{table}[htbp]
\centering
\footnotesize
\caption{Pooled out-of-fold \ac{AUC} across the temporal-first ablation sequence. Balanced
  accuracy and the static single-visit probe are reported at the seed-averaged out-of-fold
  threshold.}
\label{tab:rq3-scorecard}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}lcccc@{}}
\toprule
Configuration & $n$ & Pooled out-of-fold \ac{AUC} & Balanced acc.\ & Static $N{=}1$ \\
\midrule
S0-demo (age, sex) & 248 & $0.5296 \pm 0.0000$ & 0.5139 & 0.5296 \\
S0c \ac{GELSTM}, random encoder & 248 & $0.5625 \pm 0.0292$ & 0.5631 & 0.5140 \\
S0d Brain-TokenGT & 248 & $0.6207 \pm 0.0338$ & 0.5889 & 0.5353 \\
S0a logistic regression on drift & 248 & $0.7053 \pm 0.0000$ & 0.6776 & 0.5653 \\
S0b \ac{GELSTM}, frozen pretrained encoder & 248 & $0.7186 \pm 0.0334$ & 0.6505 & 0.4699 \\
\textbf{S1 (selected)} & 248 & $\mathbf{0.7488 \pm 0.0033}$ & 0.7093 & 0.4919 \\
S1b, node-\ac{LSTM} self-supervised init. & 248 & $0.7502 \pm 0.0125$ & 0.7037 & 0.5072 \\
S1c, graph reconstruction auxiliary loss & 248 & $0.5433 \pm 0.0311$ & 0.5477 & 0.5213 \\
S2, temporal gate & 248 & $0.7308 \pm 0.0160$ & 0.6811 & 0.4969 \\
S3, residual fusion (structurally inert) & 248 & $0.7488 \pm 0.0033$ & 0.7093 & 0.4919 \\
S4, attentive pooling & 248 & $0.6490 \pm 0.0144$ & 0.6111 & 0.5197 \\
S5, dual-score head & 248 & $0.7331 \pm 0.0173$ & 0.6885 & 0.5010 \\
SENS (minimum three visits) & 140 & $0.7413 \pm 0.0137$ & 0.6907 & 0.5469 \\
\bottomrule
\end{tabular}%
}
\end{table}

\paragraph{Tier-2 verdicts.} \autoref{tab:rq3-verdicts} applies the keep-or-drop rule of
\autoref{sec:protocol} to each configuration against S1.
$\Delta$ is always configuration minus S1, so a positive number means the challenger is better, and
the ratio $(\Delta \div \text{SE})$ indicates whether the difference is real or noise:
$|\text{ratio}| \gtrsim 2$ is real; anything smaller is noise.
\autoref{tab:rq3-col-guide} explains what each column measures and what the decision
thresholds mean.

\begin{table}[htbp]
\centering
\footnotesize
\caption{How to read each column of \autoref{tab:rq3-verdicts}.}
\label{tab:rq3-col-guide}
\begin{tabularx}{\textwidth}{@{}p{3.0cm}Xp{2.6cm}p{2.6cm}@{}}
\toprule
Column & What it measures & Candidate kept if & S1 retained if \\
\midrule
Fold-matched $\Delta \pm$ standard error (ratio) &
  Mean paired per-fold \ac{AUC} difference within each seed. Pairing cancels fold
  difficulty, so the standard error is small and a small difference reaches a large ratio &
  Positive $\Delta$ with ratio $\gtrsim +2$ &
  Ratio $\lesssim -2$, or the pooled check below fails \\[4pt]
Pooled $\Delta \pm$ standard error (ratio) &
  Per-seed difference of pooled out-of-fold \ac{AUC}, unpaired. The standard error is
  larger, so this is the conservative reading &
  Positive $\Delta$ with ratio $\gtrsim +2$ &
  Ratio $\lesssim -2$, or disagreement + tie-breaker \\[4pt]
Keep-or-drop reading &
  The pre-specified combination of both &
  Both statistics positive beyond twice their standard error $\to$ candidate kept &
  Either statistic negative beyond twice its standard error $\to$ dropped; disagreement
  $\to$ the one-standard-error tie-breaker retains the simpler model \\
\bottomrule
\end{tabularx}
\end{table}

\noindent
The two statistics measure the same quantity at different sensitivity: S1b's
fold-matched standard error is $0.0019$ while its pooled standard error is $0.0074$, which
is why the same small difference ($+0.012$ against $+0.0014$) passes one threshold and
fails the other.

\begin{table}[htbp]
\centering
\footnotesize
\caption{Tier-2 keep-or-drop statistics for each configuration against S1. Ratio is the mean
  difference divided by its standard error.}
\label{tab:rq3-verdicts}
\begin{tabularx}{\textwidth}{@{}lccX@{}}
\toprule
Contrast & Fold-matched $\Delta \pm$ standard error (ratio) & Pooled $\Delta \pm$ standard error (ratio) & Keep-or-drop reading \\
\midrule
S1b vs.\ S1 & $+0.0120 \pm 0.0019$ (+6.46) & $+0.0014 \pm 0.0074$ (+0.19) &
  Statistics disagree $\to$ one-standard-error tie-breaker: a difference of $0.0014 \ll$ one standard error $(0.0074)$ $\to$ keep the simpler S1 \\[3pt]
S1c vs.\ S1 & $-0.1587 \pm 0.0099$ (--15.98) & $-0.2055 \pm 0.0150$ (--13.72) &
  Negative on both readings $\to$ dropped \\[3pt]
S2 vs.\ S1 & $-0.0064 \pm 0.0075$ (--0.85) & $-0.0180 \pm 0.0086$ (--2.08) &
  Pooled reading is negative beyond twice its standard error $\to$ dropped \\[3pt]
S3 vs.\ S1 & $0.0000 \pm 0.0000$ & $0.0000 \pm 0.0000$ &
  Bit-identical to S1; structurally inert, no result \\[3pt]
S4 vs.\ S1 & $-0.0558 \pm 0.0058$ (--9.60) & $-0.0999 \pm 0.0083$ (--11.96) &
  Negative on both readings $\to$ dropped \\[3pt]
S5 vs.\ S1 & $-0.0046 \pm 0.0067$ (--0.68) & $-0.0158 \pm 0.0101$ (--1.56) &
  Classification-neutral; kept only as an interpretability tool, not on performance \\
\bottomrule
\end{tabularx}
\end{table}

No variant improves on S1; the reasoning for each configuration is recorded in \autoref{tab:rq3-verdicts}, so S1 is the configuration carried forward.


\subsection{Parameter Count of the Selected Configuration}\label{sec:rq3-capacity}

\paragraph{The selected configuration is not explained by capacity.}
\autoref{tab:rq3-capacity} reports total trainable parameters for the selected configuration
alongside the two configurations above it in \ac{AUC} that inherit S1's node-shared recurrent core
without a graph-propagation stage, and the two spatial-first floors it is compared against.

\begin{table}[htbp]
\centering
\caption{Total trainable parameters against pooled out-of-fold \ac{AUC} for the selected
  configuration, the two configurations closest to it in \ac{AUC}, and the two spatial-first floors.}
\label{tab:rq3-capacity}
\begin{tabular}{@{}lcc@{}}
\toprule
Configuration & Trainable parameters & Pooled out-of-fold \ac{AUC} \\
\midrule
\textbf{S1 (selected)} & \textbf{68{,}417} & \textbf{0.7488} \\
S5, dual-score head & 68{,}482 & 0.7331 \\
S2, temporal gate & 72{,}642 & 0.7308 \\
S0b, frozen pretrained spatial-first & 520{,}905 & 0.7186 \\
S0c, randomly initialised spatial-first & 965{,}897 & 0.5625 \\
\bottomrule
\end{tabular}
\end{table}

Under S1's configuration, no reconstruction target is set, so no graph autoencoder module is
constructed and the selected configuration contains no graph-propagation stage at all: it is
a node-shared \ac{LSTM} over per-region trajectories, mean-pooled, followed by a linear
classification head. It exceeds the randomly initialised spatial-first floor, which shares
no pretraining, by 0.186 \ac{AUC} at 14.1 times fewer parameters, and exceeds the pretrained
spatial-first floor by 0.030 \ac{AUC} at 7.6 times fewer trainable parameters, so the
advantage over both floors cannot be attributed to model capacity. The same table addresses the opposite direction: S2 and S5 each add parameters on top of S1's own configuration and both score lower, so additional capacity on top of the selected configuration does not improve it either. We plot the parameter-count against out-of-fold \ac{AUC} frontier and the corresponding multi-metric comparison in \autoref{fig:rq3-param-efficiency}, which places the temporal-first configurations at or above the discriminability of the spatial-first and graph-transformer baselines at a smaller parameter count.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.92\textwidth]{fig5_param_efficiency_frontier}
  \caption{Parameter efficiency frontier and multi-metric performance profile. (A) Trainable parameter count on a logarithmic scale against held-out test \ac{AUC} across model configurations, highlighting the efficiency frontier achieved by the compact temporal-first architectures (S1, S2, S5) relative to the overparameterised spatial-first and graph-transformer baselines. (B) Multi-metric radar profile comparing \ac{AUC}, F1 score, balanced accuracy, specificity, sensitivity, and parameter efficiency across representative architectures.}
  \label{fig:rq3-param-efficiency}
\end{figure}

\subsection{Matched-Window Comparison with Brain-TokenGT}\label{sec:rq3-matched-window}

\paragraph{Matched-window comparison.} \autoref{tab:rq3-w3} restricts the trajectory models
and Brain-TokenGT to the two-to-three-visit window Brain-TokenGT is architecturally
constrained to, on the pooled protocol, holding the subject pool fixed across all three
models as set up in \autoref{sec:comparability}.

\begin{table}[htbp]
\centering
\small
\caption{Pooled out-of-fold \ac{AUC} restricted to the two-to-three-visit window ($T \in
  [2,3]$), pooled protocol.}
\label{tab:rq3-w3}
\begin{tabular}{@{}lc@{}}
\toprule
Configuration (matched window) & Pooled out-of-fold \ac{AUC} \\
\midrule
Brain-TokenGT & $0.6207 \pm 0.0338$ \\
\ac{GELSTM}, random encoder & $0.5885 \pm 0.0304$ \\
\ac{GELSTM}, frozen pretrained encoder & $0.7500 \pm 0.0138$ \\
Selected temporal-first configuration & $0.7318 \pm 0.0348$ \\
\bottomrule
\end{tabular}
\end{table}

Fold-matched against the frozen pretrained spatial-first model at the same window, the
selected configuration scores $-0.0244 \pm 0.0027$ (ratio --8.94), a consistent loss across
all four seeds; against Brain-TokenGT at the same window it scores $+0.0999 \pm 0.0162$
(ratio +6.17). \autoref{tab:rq3-truncation} isolates what truncation itself does to each
architecture by comparing each model's full-trajectory score from \autoref{tab:rq3-scorecard}
against its matched-window score.

\begin{table}[htbp]
\centering
\caption{Change in pooled out-of-fold \ac{AUC} from truncating to the two-to-three-visit
  window.}
\label{tab:rq3-truncation}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}lccc@{}}
\toprule
Configuration & Full trajectory & Matched window & Change \\
\midrule
\ac{GELSTM}, frozen pretrained (spatial-first) & 0.7186 & 0.7500 & $+0.0314$ \\
Selected temporal-first configuration & 0.7488 & 0.7318 & $-0.0170$ \\
\ac{GELSTM}, random encoder & 0.5625 & 0.5885 & $+0.0260$ \\
\bottomrule
\end{tabular}%
}
\end{table}

Truncation moves the two architecture families in opposite directions: the spatial-first
models gain from truncation while the temporal-first configuration loses performance. Within this ablation sequence, the temporal-first advantage over spatial-first is observed only in the long-trajectory regime, and the spatial-first advantage is observed only in the regime truncated to the window a competing published architecture requires; a comparison that discards the additional visits the temporal-first design exists to exploit is reported here as a secondary comparison, and the primary evidence on the flip in pooling order remains the untruncated comparison. We report threshold-free evaluation across the full operating range in \autoref{fig:rq4-roc-pr}, comparing the multi-seed \ac{ROC} and \ac{PR} curves with their empirical standard deviation envelopes.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.88\textwidth]{fig2_roc_and_pr_curves}
  \caption{Multi-seed \ac{ROC} and \ac{PR} curves on the held-out test cohort across four seeds ($n=4$). (A) \ac{ROC} curves for \ac{GELSTM} with a frozen encoder and Brain-TokenGT, with solid lines indicating the seed-averaged curve, shaded bands showing $\pm 1$ standard deviation, and individual seeds plotted in semi-transparent lines alongside the chance floor ($0.50$). (B) Corresponding \ac{PR} curves evaluated against the converter prevalence baseline ($0.44$).}
  \label{fig:rq4-roc-pr}
\end{figure}


\section{RQ4: Cross-Cohort Transfer}\label{sec:rq4}

This section reports the one-pass held-out reads permitted under the four-tier evaluation
protocol of \autoref{sec:protocol}, the per-cohort split between \ac{ADNI} and \ac{DELCODE}
for every configuration, and the cohort-identity probe together with the pre-specified
mitigation that was run once the probe's threshold was found to have been exceeded.

\paragraph{Per-cohort split.} \autoref{tab:rq4-percohort} reports out-of-fold \ac{AUC}
separately for the two pooled cohorts, for every configuration in \autoref{tab:rq3-scorecard}.

\begin{table}[htbp]
\centering
\small
\caption{Pooled out-of-fold \ac{AUC}, split by cohort.}
\label{tab:rq4-percohort}
\begin{tabular}{@{}lcc@{}}
\toprule
Configuration & \ac{ADNI} & \ac{DELCODE} \\
\midrule
S0-demo (age, sex) & 0.5162 & 0.5186 \\
S0c \ac{GELSTM}, random encoder & 0.5348 & 0.6067 \\
S0d Brain-TokenGT & 0.6194 & 0.6217 \\
S0a logistic regression on drift & 0.5564 & 0.9129 \\
S0b \ac{GELSTM}, frozen pretrained encoder & 0.4971 & 0.9178 \\
\textbf{S1 (selected)} & \textbf{0.6526} & \textbf{0.8741} \\
S1b, node-\ac{LSTM} self-supervised init. & 0.6624 & 0.8707 \\
S1c (corrected re-run, random encoder) & 0.5335 & 0.5589 \\
S2, temporal gate & 0.6266 & 0.8700 \\
S3, residual fusion (structurally inert) & 0.6526 & 0.8741 \\
S4, attentive pooling & 0.5415 & 0.8022 \\
S5, dual-score head & 0.6460 & 0.8530 \\
SENS (minimum three visits) & 0.6328 & 0.8986 \\
\bottomrule
\end{tabular}
\end{table}

Every model scores substantially higher on \ac{DELCODE} than on \ac{ADNI}, and the gap is
widest for the two spatial-first baselines: S0b scores near chance on \ac{ADNI} (0.4971)
and strongly on \ac{DELCODE} (0.9178), so its pooled number of 0.7186 in
\autoref{tab:rq3-scorecard} averages two markedly different regimes rather than describing
either one. This asymmetry is the reason per-cohort columns are reported for every configuration in
this chapter's headline tables rather than the pooled column alone.

\paragraph{Held-out reads.} \autoref{tab:rq4-tier4} reports the single permitted read of the
64-subject in-domain test set and the 60-subject \ac{OASIS} set, spent once on the selected
configuration and its two pre-specified secondary variants, confirmed unspent immediately beforehand.

\begin{table}[htbp]
\centering
\small
\caption{Held-out reads for the selected configuration and its pre-specified secondaries. In-domain
  test $n=64$; \ac{OASIS} $n=60$. The out-of-fold column is the same pooled figure as
  \autoref{tab:rq3-scorecard}, repeated here as the better-powered reference point.}
\label{tab:rq4-tier4}
\begin{tabular}{@{}llccc@{}}
\toprule
Configuration & Role & In-domain test & \ac{OASIS} & Out-of-fold (reference) \\
\midrule
\textbf{S1} & primary & $\mathbf{0.7909 \pm 0.0162}$ & $\mathbf{0.4892 \pm 0.0224}$ & $0.7488 \pm 0.0033$ \\
S1b & secondary & $0.7760 \pm 0.0568$ & $0.4602 \pm 0.0274$ & $0.7502 \pm 0.0125$ \\
S5 & secondary & $0.7870 \pm 0.0139$ & $0.5070 \pm 0.0109$ & $0.7331 \pm 0.0173$ \\
\bottomrule
\end{tabular}
\end{table}

The in-domain test \ac{AUC} for S1 and S5 lands above the prediction interval implied by their own out-of-fold scores. This is reported as a fact about this single 64-subject read rather than as a second, independent win: the plan's own noise floor at this sample size is approximately 0.08 \ac{AUC}, which covers the gap, and the 248-subject out-of-fold pool remains the better-powered point estimate; no headline claim in this thesis is updated on the in-domain test read alone. We report the exact cell-level classification breakdown across seeds in \autoref{fig:rq4-confusion-matrices}, displaying the 2-by-4 confusion matrix grid at the respective operating thresholds.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{fig3_confusion_matrices}
  \caption{Multi-seed confusion matrices grid on the held-out test cohort ($n=25$) across seeds 42, 43, 44, and 45. Top row: \ac{GELSTM} with a frozen encoder across operating thresholds selected via out-of-fold F1 optimisation. Bottom row: Brain-TokenGT across the same four seeds. True stable \ac{MCI} ($n=14$) and converters ($n=11$) are enumerated per cell.}
  \label{fig:rq4-confusion-matrices}
\end{figure}

The \ac{OASIS} result is the load-bearing external-transfer read. All three models sit within
noise of chance: the per-seed standard error at $n \approx 60$ is approximately 0.075, and
each model's four-seed mean is within about one standard error of 0.5 (S1 ranges 0.4705 to
0.5217 across its four seeds; S1b ranges 0.4416 to 0.5006; S5 ranges 0.4972 to 0.5217).
Cross-cohort transfer to \ac{OASIS} was at chance under the evaluated protocol, against
0.77 to 0.79 in-domain \ac{AUC} for the same three models; this is reported as no evidence of
transfer to an unseen cohort, not as evidence of below-chance failure, which the data do not
show. We evaluate the probabilistic fidelity of these test predictions in \autoref{fig:rq4-calibration} via reliability curves and Brier score loss, verifying that in-domain baseline predictions maintain calibrated risk scores across seeds prior to external domain shift.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\textwidth]{fig4_probability_calibration}
  \caption{Probability calibration and Brier score evaluation on the held-out test set. (A) Reliability diagram comparing predicted conversion probabilities against observed empirical conversion frequencies for \ac{GELSTM} and Brain-TokenGT relative to perfect calibration. (B) Brier score loss across seeds 42 to 45, measuring the mean squared difference between predicted probabilities and observed binary outcomes.}
  \label{fig:rq4-calibration}
\end{figure}

\paragraph{Cohort-identity probe.} A logistic probe is fitted on the pooled patient latent
to decode cohort of origin (\ac{ADNI} against \ac{DELCODE}), with a pre-specified escalation
threshold of $0.75$ fixed in \autoref{sec:protocol}. \autoref{tab:rq4-probe} reports its
out-of-fold decoding \ac{AUC} for every configuration.

\begin{table}[htbp]
\centering
\small
\caption{Cohort-identity probe out-of-fold \ac{AUC} against the pre-specified escalation
  threshold of 0.75.}
\label{tab:rq4-probe}
\begin{tabularx}{\textwidth}{@{}lcX@{}}
\toprule
Configuration & Cohort-probe \ac{AUC} & Exceeds threshold \\
\midrule
S1 (selected) & $0.860 \pm 0.010$ & yes \\
S1b & $0.890$ & yes \\
S1c, corrected re-run & $0.860$ (identical to S1) & yes \\
S2 & $0.869$ & yes \\
S3 & $0.860$ (identical to S1) & yes \\
S4 & $0.847$ & yes \\
S5 & $0.863$ & yes \\
SENS & $0.742$ & no, just under threshold at a smaller pool \\
\bottomrule
\end{tabularx}
\end{table}

Every full-pool configuration decodes cohort of origin at approximately 0.86 \ac{AUC} despite no
cohort-conditioning term in the training objective, meaning the model encodes cohort
identity as a side effect of learning to classify rather than as an intended input. The
strong decodability of cohort identity from the learned representation is consistent with
substantial cohort dependence and may contribute to the poor transfer to \ac{OASIS} reported
above; \ac{OASIS} is exactly the unseen cohort category such a dependence would be expected
to fail on. No causal claim is drawn from the probe result alone, since no experiment in
this ablation sequence isolates the probe's contribution from other differences between the cohorts.

\paragraph{Escalation trigger: protocol deviation and remediation.} The pre-specified
escalation trigger had been exceeded from the first runs onward and was not read
against its own $0.75$ threshold until the Tier-4 pass assembled every configuration's probe value
together. The probe value itself was computed and persisted correctly throughout, so no data
was lost; the rule was simply not applied at the point it should have been. This is recorded
as a process gap: every in-domain out-of-fold result reported in this chapter is unaffected
by it, since these are out-of-fold quantities that do not depend on cross-cohort transfer and
were not selected using the probe.

Once found, the pre-specified remedy was run exactly as specified in \autoref{sec:tfgn}: a gradient-reversal
cohort-adversarial head attached to the same pooled embedding the probe scores, every other
setting identical to S1, across all four seeds. \autoref{tab:rq4-adversarial} reports the
outcome on both axes the mitigation targets.

\begin{table}[htbp]
\centering
\caption{Pre-specified gradient-reversal mitigation, against the S1 baseline it modifies.}
\label{tab:rq4-adversarial}
\begin{tabular}{@{}lcc@{}}
\toprule
Configuration & Pooled out-of-fold \ac{AUC} & Cohort-probe \ac{AUC} \\
\midrule
S1 (baseline) & $0.7488 \pm 0.0033$ & $0.8600 \pm 0.0074$ \\
S1 with gradient-reversal mitigation & $0.7066 \pm 0.0075$ & $0.9411 \pm 0.0131$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Intended behaviour.} The adversarial head was designed to remove cohort information from the shared embedding,
lowering the cohort-probe \ac{AUC} while keeping classification performance close to S1.
Neither held: the cohort-probe \ac{AUC} rose from $0.8600$ to $0.9411$ and the pooled out-of-fold \ac{AUC} fell from $0.7488$ to $0.7066$.

\paragraph{Summary statement.} Cross-cohort transfer to \ac{OASIS} was at chance under the
evaluated protocol ($\ac{AUC} = 0.4892 \pm 0.0224$, indistinguishable from chance),
consistent with an unescalated cohort-identity shortcut (cohort-probe \ac{AUC}
$\approx 0.86 \gg 0.75$); a pre-specified gradient-reversal mitigation was run and did not
recover transfer on either axis it targeted. Learning a cohort-invariant representation
under this pooling protocol remains an open problem rather than a solved one; it is neither
a clean external-validation result nor an unexamined trigger.

\section{Interpretability and Latent-Space Results}\label{sec:interpretability-results}

In this section we report the \ac{DELCODE}-only disease-axis and latent-separability results built on the spatial-first frozen encoder (\ac{GELSTM}) of \autoref{sec:rq2}, using the instrumentation defined in \autoref{sec:instrumentation}. Because the spatial-first architecture models whole-brain graph embeddings explicitly via a 64-dimensional latent bottleneck, it provides a direct testbed for evaluating latent disease-axis alignment and Fisher discriminant separability. In contrast, the selected temporal-first model (S1) operates directly on regional node trajectories without constructing an intermediate graph autoencoder bottleneck (\autoref{sec:rq3}).

\paragraph{Disease axis, latent separability and the logistic probe (\ac{DELCODE}-only).}
The disease axis and per-scan disease score, the Fisher discriminant ratio and the
\ac{UMAP} projection defined in \autoref{sec:instrumentation} are reported here for the
frozen spatial-first \ac{GELSTM} of \autoref{sec:rq2}, evaluated at seed 42 on the full
167-subject \ac{DELCODE} downstream cohort (99 stable \ac{MCI}, 68 converters, pooling the
cross-validation and held-out subjects for this descriptive projection). \autoref{fig:rq6-umap}
shows the resulting projection with the disease axis overlaid.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.75\textwidth]{latent_space}
  \caption{\ac{UMAP} projection of the 64-dimensional pooled graph embeddings for the full
  \ac{DELCODE} downstream cohort, with the disease axis drawn between the two group
  centroids.}
  \label{fig:rq6-umap}
\end{figure}

The \ac{UMAP} projection shows a clear directional separation between the two groups along the conversion axis. Converters concentrate toward lower UMAP-1 values while stable-\ac{MCI} subjects dominate higher values, yielding a visible displacement of group centroids ($\|\mathbf{c}_{\text{conv}} - \mathbf{c}_{\text{stable}}\|_2 = 2.59$) and a very large effect size along the conversion axis ($\text{Cohen's } d = 1.34$, $t = 9.18$, $p < 10^{-15}$). In the projected two-dimensional space, this alignment yields a silhouette score of $0.21$, whereas in the full 64-dimensional latent space the silhouette score is $0.086$, reflecting that disease-discriminative structure is concentrated along specific directional axes rather than forming universally disjoint spherical clusters. This is corroborated by the logistic-regression probe fitted on the pooled 64-dimensional embeddings (mean per-fold \ac{AUC} of $0.859$; out-of-fold \ac{AUC} of $0.849$).

The Fisher discriminant ratio, computed per latent dimension over the same pooled
embeddings, identifies dimensions 2, 29, 12, 22 and 10 (zero-indexed) as the five most
discriminative of the 64. Signal is therefore concentrated in a minority of latent
dimensions rather than smeared evenly across all 64, which is the practical distinction
behind any claim that the encoder has learned a disease-relevant direction as opposed to a
diffuse one. Latent steering along the fitted disease axis, decoding each steered point back
to a connectivity matrix through the \ac{GAAE} decoder, and visit-occlusion, removing each
visit from a single subject's trajectory and measuring the resulting change in predicted
probability, are both part of the instrumentation defined in \autoref{sec:instrumentation};
neither is used here to support a numerical claim, since each is designed as a per-subject
plausibility check rather than a cohort-level statistic, and no inference beyond the single
walkthrough subject is drawn from either alone.

\section{Robustness and Perturbation Results}\label{sec:robustness-results}

In this section we report two single-run sensitivity analyses on trained models: cohort-decision perturbations for the \ac{GAAE} reconstruction-error classifier of \autoref{sec:rq2} and a simulated scanner-drift experiment for the \ac{DELCODE} \ac{GEGRU} trajectory model. Both analyses serve as descriptive robustness checks on specific baseline representation mechanisms rather than competing full-cohort ablations.

\paragraph{Cohort perturbations.} Because the selected temporal-first model (S1) eliminates the graph autoencoder stage entirely (68{,}417 vs.\ 965{,}897 parameters), reconstruction-error perturbation specifically interrogates the representation stability of the baseline \ac{GAAE} autoencoder. We evaluate decision stability under feature noise, matrix rebuild (feature noise followed by re-thresholding the connectivity graph), edge perturbation, and demographic-conditioning noise, defined in \autoref{sec:instrumentation}. We test the five most confidently classified subjects in each cohort (healthy, \ac{AD}, \ac{MCI}, converter) across ten independent trials at 0\% to 30\% noise. Decision stability denotes the proportion of trials that preserve the original cohort assignment. At 0\% noise, all methods reproduce the baseline decision (stability of $1.00$), confirming numerical reproducibility. \autoref{tab:rq7-perturbation} reports decision stability at 0\% and 30\% noise; intermediate noise levels changed monotonically and are omitted for brevity.

\begin{table}[htbp]
\centering
\footnotesize
\caption{Cohort stability rate (proportion of ten trials preserving the original decision) at 0\% and 30\% noise across perturbation methods. One-versus-rest validation \ac{AUC} for each cohort's threshold is provided for context.}
\label{tab:rq7-perturbation}
\begin{tabular}{@{}lcccccc@{}}
\toprule
Cohort (threshold \ac{AUC}) & \multicolumn{2}{c}{Feature noise} & \multicolumn{2}{c}{Matrix rebuild} & \multicolumn{2}{c}{Edge / conditioning} \\
 & 0\% & 30\% & 0\% & 30\% & 0\% & 30\% \\
\midrule
Healthy (0.610) & 1.00 & 0.20 & 1.00 & 0.20 & 1.00 & 1.00 \\
\ac{AD} (0.541) & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 \\
\ac{MCI} (0.600) & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 \\
Converter (0.550) & 1.00 & 0.80 & 1.00 & 0.62 & 1.00 & 1.00 \\
\bottomrule
\end{tabular}
\end{table}

Cohort decisions were completely insensitive to edge perturbations and demographic conditioning noise (stability of $1.00$ across all cohorts and noise levels). Because conditioning noise alters only age and sex while keeping the graph structure and features fixed, this insensitivity indicates that reconstruction-error decisions are driven primarily by graph-derived signals rather than demographic covariates.

Only direct feature-level perturbations lowered decision stability, and did so exclusively at the highest noise level (30\%). Healthy and converter decisions showed vulnerability (healthy stability fell to $0.20$; converter stability fell to $0.80$ under feature noise and $0.62$ under matrix rebuild), whereas \ac{AD} and \ac{MCI} decisions remained at $1.00$ throughout. This asymmetry follows directly from the decision rule: healthy and converter subjects are assigned based on low reconstruction error, so feature perturbations that raise reconstruction error push borderline cases across the threshold. Conversely, \ac{AD} and \ac{MCI} subjects are assigned based on high reconstruction error, so increased error reinforces their classification. These threshold \ac{AUC} values are modest ($0.541$ to $0.610$), confirming that reconstruction error is a weak cohort discriminator; high stability reflects the margin of selected subjects from the boundary rather than strong overall discriminative capacity.

\paragraph{Scanner-drift simulation.} We apply the simulated cross-cohort drift sweep of \autoref{sec:instrumentation} to the baseline recurrent trajectory model (\ac{GEGRU}) on the \ac{DELCODE} held-out test cohort as a bounded historical sensitivity check, calibrated prior to the availability of external pooled matrices. These represent metadata-calibrated simulations rather than evaluations on real external connectivity matrices. \autoref{tab:rq7-drift} reports the resulting held-out performance.

\begin{table}[htbp]
\centering
\small
\caption{Simulated scanner-drift sweep against actual baseline performance on \ac{DELCODE} held-out test predictions. Calibrated noise levels scale the base \ac{DELCODE} inter-visit drift by $(1+c_v)$, where $c_v$ is the repetition-time coefficient of variation from the target cohort.}
\label{tab:rq7-drift}
\begin{tabularx}{\textwidth}{@{}Xccccc@{}}
\toprule
Simulation profile & Noise level & \ac{AUC} & Sensitivity & Specificity & F1 \\
\midrule
Actual baseline (no perturbation) & 0.000 & 0.793 & 0.643 & 0.600 & 0.581 \\
\ac{ADNI}-calibrated simulation (feature $+$ edge noise) & 1.371 & 0.682 & 0.000 & 1.000 & 0.000 \\
\ac{OASIS}-calibrated simulation (feature noise, half edge) & 1.348 & 0.700 & 0.071 & 1.000 & 0.133 \\
\bottomrule
\end{tabularx}
\end{table}

At low to moderate noise levels ($0.1$ to $0.5$), \ac{AUC} fluctuations were minor ($0.800\text{--}0.832$) and were not interpreted as performance gains. At calibrated drift levels (approximately $1.35\times$ base drift), \ac{AUC} decreased from $0.793$ to $0.682$ (\ac{ADNI}-calibrated) and $0.700$ (\ac{OASIS}-calibrated). 

Comparing the full diagnostic metrics reveals the underlying failure mechanism: under calibrated acquisition noise, the mean output probability drops from $0.511$ down to $0.268$ (\ac{ADNI}) and $0.301$ (\ac{OASIS}), as recurrent feature activations become attenuated. Evaluated against the fixed in-domain decision threshold, predictions revert almost entirely to the majority stable-\ac{MCI} class: specificity saturates to $1.000$ while sensitivity collapses from $0.643$ down to $0.000$ and $0.071$, driving the F1 score from $0.581$ to $0.000\text{--}0.133$.

\paragraph{Comparison with empirical cross-cohort results.} When juxtaposed with the real empirical cross-cohort evaluations of \autoref{sec:rq4}, the simulation accurately predicts the failure mode (converter sensitivity collapse) but underestimates the full severity of the transfer loss (\autoref{fig:rq7-drift-transfer}):
\begin{itemize}
  \item \textbf{Under simulated drift (Table~\ref{tab:rq7-drift})}, \ac{AUC} drops from $0.793$ to $0.682$ (\ac{ADNI}-calibrated) and $0.700$ (\ac{OASIS}-calibrated), with sensitivity falling to $0.000$ and $0.071$.
  \item \textbf{Under real cross-cohort transfer (Tables~\ref{tab:rq4-percohort} and \ref{tab:rq4-tier4})}, spatial-first recurrent representations degrade all the way to chance: \ac{ADNI} in-domain \ac{AUC} reaches only $0.4971$ (configuration S0b), and \ac{OASIS} held-out transfer reaches $0.4892 \pm 0.0224$ (configuration S1).
\end{itemize}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.88\textwidth]{fig_scanner_drift_vs_real_transfer}
  \caption{Simulated scanner drift versus real empirical cross-cohort transfer for recurrent architectures. (A) Comparison of held-out \ac{AUC} across in-domain baseline, metadata-calibrated simulated drift, and real cross-cohort transfer to \ac{ADNI} and \ac{OASIS}, relative to the chance floor ($0.50$). (B) The accompanying shift in the predicted probabilities: under increasing input variance the model's mean output probability falls from $0.511$ to $0.268\text{--}0.301$, and over the same sweep specificity rises to $1.000$ while converter sensitivity falls to $0.000\text{--}0.071$.}
  \label{fig:rq7-drift-transfer}
\end{figure}

The simulated sweep and the real cross-cohort evaluation degrade in the same direction, and the real transfer gap is the larger of the two. The simulation is calibrated on acquisition-parameter dispersion ($c_v$) alone, so the residual gap is consistent with additional sources of domain shift that the simulation does not model, such as site-specific field inhomogeneities, gradient non-linearities and motion profiles. No experiment here separates those sources.

\section{Ablations Not Run}\label{sec:ablations-not-run}

This section records ablations that were considered and deliberately not run, so that a
deferred experiment is not mistaken for one that was overlooked.

\paragraph{Scaling gate closed.} The pre-specified gate for a scaled second block of
experiments required a cumulative gain, from S1c through S5, exceeding the standard error
of the seed-level differences. \autoref{tab:rq3-verdicts} shows the outcome: $-0.1587$
(S1c), $-0.0064$ (S2), structurally inert (S3), $-0.0558$ (S4) and $-0.0046$ (S5); no configuration
above S1 was kept. The scaled block does not run. Read together with the parameter counts
of \autoref{tab:rq3-capacity}, the conclusion the gate itself specified is that signal
quality and sample size, rather than model capacity, are the limiting factor at this cohort
size, and a configuration 14 times smaller than the baseline it exceeds is the evidence for
that conclusion rather than merely consistent with it.

\paragraph{A reconstruction-weight sweep for S1c.} S1c's corrected re-run collapses
classification performance by approximately 0.20 \ac{AUC} relative to S1
(\autoref{sec:rq3}). A sweep over the reconstruction loss weight might recover some of this
loss, but was not run: searching a hyperparameter after observing the result it might rescue
is exactly the undocumented re-run the pre-specification exists to prevent.

\paragraph{An encoder-learning-rate-scale configuration for the fine-tuned encoder ablation.} The
fine-tuned model of \autoref{tab:rq2-encoder} trains the unfrozen pretrained encoder at the
same learning rate as the freshly initialised classification head. A variant that applies a
reduced, discriminative learning rate to the encoder, analogous to the tenfold-reduced scale
Brain-TokenGT applies to its own newly unfrozen parameters in \autoref{sec:braintokengt},
would test the leading candidate mechanism for this model's instability directly. It was not
run before the writing deadline and remains a falsifiable, deferred test of that mechanism.

\paragraph{A gradient-magnitude comparison for the cohort-adversarial mitigation.} The
gradient-reversal mitigation of \autoref{sec:rq4} raised the cohort-identity probe's
decodability rather than lowering it. A comparison of gradient magnitudes between the
classification and adversarial objectives would test the leading candidate explanation, that
the reversed gradient is unweighted relative to the classification loss, but was not run.

\paragraph{A second held-out read for the cohort-adversarial mitigation.} The
gradient-reversal mitigation was scored only on the out-of-fold pool. Spending the one-shot
held-out test or \ac{OASIS} read on a configuration the Tier-2 stopping rule already rejects on
out-of-fold evidence was deliberately not done, consistent with the scope table of
\autoref{sec:protocol} restricting the held-out reads to models that pass Tier 2.

\paragraph{The fusion contrast.} S3's fusion setting is structurally inert under S1's
configuration, and its only available parent under a self-supervised reconstruction target,
S1c, collapses. Branching a fusion variant from a parent configuration that does not itself
train usefully would answer nothing about whether fusion helps; the fusion question
consequently remains untestable within this ablation sequence rather than answered in the negative.

\paragraph{A model-side inter-visit-interval ablation.} A dedicated ablation of the
inter-visit-interval input (holding it fixed against shuffling visit order, as a test of
whether the recurrence uses timing as well as sequence) was previously run under a
configuration that has since been identified as sharing an encoder swap and a downstream
hyperparameter change with other results excluded from this thesis. It is not re-reported
here. The cohort-level fact this ablation would have tested against remains available
without any model in the loop: \ac{DELCODE}'s recorded inter-visit intervals are 90.0\% at
twelve months, 7.1\% at twenty-four, 2.6\% at thirty-six and 0.4\% at forty-eight
(\autoref{sec:comparability}), so there is little timing variation on this cohort for such
an ablation to isolate in the first place; a cohort with more irregular spacing, such as
\ac{ADNI} or \ac{OASIS}, would be needed to test the interval input's contribution directly.
