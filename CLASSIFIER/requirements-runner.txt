# Extra dependencies for the experiment runner (run_experiment.py).
# Install on top of the project-root .venv (torch / torch_geometric / numpy live there).
papermill>=2.5
ipykernel>=6.29
wandb>=0.17
PyYAML>=6.0
optuna>=3.6
