% Standalone preview of the four-tier evaluation protocol funnel (D1).
% Mirrors THESIS/settings.tex's package load order, TUM colour definitions, and
% light/dark theme switch so the figure previews exactly as it would in the
% thesis, without touching THESIS/ itself. Compile:
%   tectonic -o build preview.tex        (light theme)
%   tectonic -o build preview_dark.tex   (dark theme, via \jobname switch)

\documentclass[fontsize=11pt,paper=a4]{scrbook}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage[sc]{mathpazo}
\usepackage{graphicx}
\usepackage{array}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc}
\usepackage{caption}
\usepackage[printonlyused]{acronym}
\usepackage{ifthen}
\usepackage{hyperref}
\hypersetup{hidelinks}

% Light/dark theme switch, identical mechanism to THESIS/settings.tex.
\ifthenelse{\equal{\detokenize{dark}}{\jobname} \OR \equal{\detokenize{preview_dark}}{\jobname}}{%
  \newcommand{\bg}{black}
  \newcommand{\fg}{white}
  \usepackage[pagecolor=\bg]{pagecolor}
  \color{\fg}
}{%
  \newcommand{\bg}{white}
  \newcommand{\fg}{black}
}

% TUM corporate design colours, copied from THESIS/settings.tex.
\definecolor{TUMBlue}{HTML}{0065BD}
\definecolor{TUMSecondaryBlue}{HTML}{005293}
\definecolor{TUMSecondaryBlue2}{HTML}{003359}
\definecolor{TUMBlack}{HTML}{000000}
\definecolor{TUMWhite}{HTML}{FFFFFF}
\definecolor{TUMDarkGray}{HTML}{333333}
\definecolor{TUMGray}{HTML}{808080}
\definecolor{TUMLightGray}{HTML}{CCCCC6}
\definecolor{TUMAccentGray}{HTML}{DAD7CB}
\definecolor{TUMAccentOrange}{HTML}{E37222}
\definecolor{TUMAccentGreen}{HTML}{A2AD00}
\definecolor{TUMAccentLightBlue}{HTML}{98C6EA}
\definecolor{TUMAccentBlue}{HTML}{64A0C8}

\begin{document}

\pagecolor{\bg}
\color{\fg}

\begin{acronym}
  \acro{AUC}[AUC]{area under the receiver operating characteristic curve}
  \acro{OASIS}[OASIS-3]{Open Access Series of Imaging Studies, third release}
\end{acronym}

\noindent\textbf{Preview page geometry:} \the\textwidth\ (compare against the thesis
\texttt{scrbook} textwidth before sizing \texttt{\textbackslash resizebox} in
\texttt{THESIS/chapters/04\_methodology.tex}).

\begin{figure}[p]
  \centering
  \resizebox{0.86\textwidth}{!}{\input{fig_protocol_funnel}}
  \caption{The four-tier evaluation protocol. Candidate architectures must clear
    Tier~1 (floor gates), Tier~2 (selection rule), and Tier~3 (robustness vetoes)
    before Tier~4 reads the one-shot held-out test sets (in-domain and external
    OASIS-3). Per-configuration eligibility across tiers is tabulated in
    \autoref{tab:protocol-scope}.}
  \label{fig:protocol-funnel}
\end{figure}

\end{document}
